generator:
  class: sfDoctrineGenerator
  param:
    model_class:           usuario
    theme:                 admin
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          usuario
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:  ~
      list:
        display: [ nombre, email, tipo_identificacion, identificacion, foto, estado, fecha_nacimiento, fecha_creacion]
      filter: 
        display: [ nombre, email, tipo_identificacion, identificacion, estado, fecha_nacimiento, fecha_creacion]
      form:    ~
      edit:    ~
      new:     ~
